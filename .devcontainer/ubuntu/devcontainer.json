{
  "image": "mcr.microsoft.com/devcontainers/base:resolute",
  "postCreateCommand": "mkdir -p \"$HOME/.local/bin\" && sh -c \"$(curl -fsLS https://get.chezmoi.io)\" -- -b \"$HOME/.local/bin\" init --apply carlospedreira --override-data '{\"profile\":\"container\"}'"
}
