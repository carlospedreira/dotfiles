#!/usr/bin/env bash
set -euo pipefail

rm -rf "$HOME/dotbot"

cd "$HOME/Developer/gh/andresharpe/dotbot"
pwsh install.ps1
killall pwsh 2>/dev/null || true

cd "$HOME/Developer/tmp"
if [ -d botdot ]; then
    rm -rf botdot
fi
mkdir botdot

cd botdot
dotbot init
pwsh .bot/go.ps1
